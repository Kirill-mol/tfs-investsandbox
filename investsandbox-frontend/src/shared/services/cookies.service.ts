import { ICookies } from './../interfaces/ICookies';
import { Injectable } from '@angular/core';

@Injectable({providedIn: 'root'})
export class CookiesService implements ICookies {
  constructor() { }
  
}